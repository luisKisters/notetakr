import XCTest

final class NoteTakrLaunchTests: XCTestCase {
    func testAppTargetCompiles() {
        // Smoke test: verifies the test bundle loads and the app target builds successfully.
        XCTAssertTrue(true)
    }
}
